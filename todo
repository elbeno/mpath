Allow merging in more path parts, immutably

    mpaths.charSrc.extend(charName, defAttireDir, defAttire)

P4 Path powers

    mpath["path"].inP4()
                 .opened() -> [], [sdas, nnacu]
                 .openedBy("sdas") -> False
                 .lastEdited()

helps for everything

    tabs unsupported (but probably work)
    no backslashes

