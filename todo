MPath

    store as parts

mpath.fromLayout()

    enableDot=True
    no tabs
    no backslashes
    no spaces, beyond indentation
    pipe separation

help(mpath), etc

path methods (on dot syntax):

    mpath.path.exists()
              .isFile()
              .isDir()
              .inP4()
              .opened() -> [], [sdas, nnacu]
              .openedBy("sdas") -> False
              .lastEdited()

